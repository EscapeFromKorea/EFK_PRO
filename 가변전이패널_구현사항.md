# 가변 전이 패널 — 이동/회전 확장 설계 명세

## 문서 정보

- 대상 기능: 기존 `PortalSurface`("가변 전이 패널", `Assets/SpacePortalSystem/PortalSurface.cs`)의
  이동/회전 확장 — 포탈을 설치할 수 있는 패널 자체가 움직이거나 회전하는 실험용 변형.
- 엔진/버전: Unity 2022.3.62f3 (3D)
- 언어: C#
- 문서 범위: **패널 자신의 이동/회전 상태·구동 계약·안전장치**까지만 다룬다. 레버·전력 공급·
  플레이어 물리력 등 구체적인 구동 입력 구현과, 라이더 운반의 정확한 코드 경로는 이 문서의
  범위 밖이며 별도 후속 작업에서 다룬다(태엽 축 설계 명세, `태엽축_구현사항.md`와 같은 범위
  분리 원칙).
- 목적: 이 문서만으로 Claude Code가 이동/회전 패널 컴포넌트를 구현할 수 있도록, 확정된 사항과
  미확정 사항을 명확히 구분해 기술한다.
- 전제: `docs/PRD/SpacePortal.md`가 정본이며, 이 문서는 그 §4.4("가변 전이 패널 — 움직이는
  발판에도 처음부터 허용")를 실제로 "패널 자신이 구동원이 되는" 수준까지 확장한다.

---

## 1. 원본 기획 요구사항 (참고용 원문)

```
### 기능

포탈을 설치할 수 있으며, 설치된 포탈을 유지한 채 이동하거나 회전할 수 있는 실험용 패널이다.

### 요구사항

- 포탈 설치가 가능한 전용 표면을 가진다.
- 포탈이 설치된 상태로 패널을 이동·회전할 수 있어야 한다.
- 레일 이동형과 회전축형을 지원한다.
- 레버, 전력 공급, 플레이어의 물리력 등으로 작동할 수 있어야 한다.
- 이동 거리, 이동 속도, 회전 범위, 회전 속도를 조절할 수 있어야 한다.
- 패널을 연속 각도에서 정지시킬 수 있어야 한다.
- 특정 각도에만 스냅되는 기능은 선택적으로 적용한다.
- 정사면체나 블록을 끼워 패널의 움직임을 물리적으로 방해할 수 있어야 한다.
- 물체가 끼었을 때 무한 진동이나 Joint 폭주가 발생하지 않아야 한다.
- 이동 중 플레이어를 밀거나 함께 운반할 수 있어야 한다.
- 패널 위 포탈을 통과하는 순간에도 출구 위치와 방향이 정확해야 한다.
- 패널 이동 경로와 회전축을 에디터에서 확인할 수 있어야 한다.
- 패널이 고착됐을 때 초기 위치로 복구하는 기능을 제공한다.

### 주요 활용

- 포탈 출구 위치와 각도 변경
- 상층 출구 연결
- 경사로와 임시 다리 제작
- 포탈 발사 방향 조절
- 플레이어와 코어 운반
- 정사면체 쐐기 및 정육면체 무게 고정

### 완료 조건

- 이동형·회전형 패널이 각각 정상 작동한다.
- 패널 이동 중에도 설치된 포탈이 유지된다.
- 움직이는 포탈을 플레이어와 물체가 정상적으로 통과할 수 있다.
- 패널을 여러 각도에서 멈춰 실제 경사로나 발사대로 사용할 수 있다.
- 물체가 패널에 끼어도 물리 시스템이 폭주하지 않는다.
```

---

## 2. 핵심 모델 정의

태엽 축이 "충전-방전 축적기"였다면, 이 패널은 **"진행도 기반 위치 제어기
(Progress-driven Positioner)"**로 동작한다. 입력이 들어오는 동안 정규화 진행도 `t`(0~1)가
목표 방향으로 이동/회전 속도만큼 변하고, 입력이 멈추면 `t`도 그 즉시 멈춘다. 태엽 축처럼
스스로 방전되며 계속 움직이지 않는다 — "패널을 연속 각도에서 정지시킬 수 있어야 한다"는
요구사항 자체가 관성 없는 즉시 정지를 전제하기 때문이다.

이 패널이 책임지는 범위:

1. 구동 입력(부호 있는 delta)을 받아 진행도 `t`를 갱신한다.
2. `t`를 실제 Transform으로 변환한다(Rail 모드=위치 보간, Pivot 모드=회전 보간).
3. 다음 위치로 전진하기 전에 장애물(끼임)을 감지해, 막혀 있으면 힘으로 밀어붙이지 않고
   전진 자체를 보류한다.
4. 위에 탄 라이더(플레이어)를 자신의 이동/회전에 맞춰 함께 옮긴다.
5. 장시간 고착 시 초기 위치(`t=0`)로 복구하는 절차를 제공한다.

이 패널이 모르는/관여하지 않는 범위:

- 구동 입력이 레버인지, 전력 공급인지, 플레이어가 미는 물리력인지 — `ApplyDrive(float
  signedDelta)` 계약 하나로 통일한다(태엽 축의 `ApplyRotation(float signedDelta)`와 동일
  철학, §4 참고).
- 설치된 `SpacePortal`을 직접 참조하거나 움직이지 않는다. `docs/PRD/SpacePortal.md` §4.4가
  이미 "포탈은 설치 시 자신을 표면의 자식 Transform으로 붙인다"고 확정했으므로, 이 패널은
  **자신의 Transform만 옮기면** 그 위의 포탈은 부모-자식 관계로 자동으로 따라온다. 포탈 쪽
  코드를 한 줄도 고칠 필요가 없다는 것이 이 설계의 핵심 전제다(§8에서 이 전제의 미검증
  구간을 별도로 짚는다).

---

## 3. 상태 모델

| 필드 | 타입 | 설명 |
|---|---|---|
| `mode` | `enum { Rail, Pivot }` | 이동형/회전형 선택. 패널 하나는 하나의 모드만 가진다 |
| `progress` (`t`) | `float` | 0~1 정규화 진행도. 0=시작 자세, 1=목표 자세 |
| `isDriving` | `bool` | 이번 프레임에 실제로 구동 입력이 들어왔는지 |
| `isJammed` | `bool` | 가장 최근 전진 시도가 장애물에 막혀 있는 중인지 |
| `jammedSince` | `float` | `isJammed`가 true가 된 시각(`Time.time`) — 고착 복구 타이머 기준 |

### Rail 모드 전용

| 필드 | 설명 |
|---|---|
| `startPose` / `endPose` | 이동 시작/끝 지점(로컬 오프셋 또는 별도 Transform 마커 2개). 방향은 둘을 잇는 직선 |
| `travelSpeed` | 초당 진행도 변화량 상한(= 초당 실제 이동 거리 ÷ 두 지점 사이 거리) |

### Pivot 모드 전용

| 필드 | 설명 |
|---|---|
| `pivotPoint` / `pivotAxis` | 회전 중심과 축(로컬 기준) |
| `minAngle` / `maxAngle` | 회전 범위(도), `t=0`→`minAngle`, `t=1`→`maxAngle` |
| `rotationSpeed` | 초당 각속도 상한 |

두 모드가 필드는 다르지만 **`progress` 하나로 상태를 표현한다는 점은 동일**하다 — "연속
정지"·"선택적 스냅"(§5)·"장애물 감지"(§6) 로직을 모드에 상관없이 공유하기 위한 설계다.

---

## 4. 입력 경계 (Input Boundary)

```csharp
public void ApplyDrive(float signedDelta)
```

- `signedDelta`: 이번 프레임(또는 이번 호출)에 요청된 부호 있는 진행도 변화 요청량. 양수 =
  `t` 증가 방향, 음수 = `t` 감소 방향.
- 레버/전력 공급/플레이어의 물리력 중 무엇이 호출하든 이 메서드 하나만 거치면 된다 — 구체
  드라이버 구현은 이 문서의 범위 밖(§14).
- 이 메서드가 호출되는 프레임 동안 `isDriving = true`로 간주한다(다음 프레임에 호출이 없으면
  자동으로 false).
- 태엽 축과의 결정적 차이: **이 패널은 입력이 끊기면 즉시 멈춘다.** 축은 손을 떼도
  `dischargeRate`만큼 계속 방전되며 스스로 움직였지만, 이 패널은 자체 방전/감쇠가 없다 —
  "패널을 연속 각도에서 정지시킬 수 있어야 한다"는 요구사항이 임의의 `t`에서 관성 없이 딱
  멈추는 동작을 요구하기 때문이다.

---

## 5. 진행 로직 — 연속 정지 + 선택적 스냅

```csharp
void Tick(float dt)
{
    if (isDriving && !isJammed)
    {
        float speedLimit = mode == Mode.Rail ? travelSpeed : rotationSpeedAsProgressRate;
        float delta = Mathf.Clamp(requestedDelta, -speedLimit * dt, speedLimit * dt);

        float candidate = Mathf.Clamp01(progress + delta);
        if (WouldBeBlocked(candidate)) { isJammed = true; jammedSince = Time.time; }
        else { progress = candidate; isJammed = false; }
    }

    ApplyProgressToTransform(progress); // Rail: Lerp(startPose, endPose, progress)
                                         // Pivot: pivotAxis 기준 Lerp(minAngle, maxAngle, progress)

    isDriving = false;
}
```

- `progress`는 입력이 없으면 절대 스스로 변하지 않는다(§4의 "연속 정지" 요구사항을 코드
  수준에서 보장).
- **스냅은 선택 기능이며, `progress` 자체를 바꾸지 않고 Transform 적용 단계에서만 개입한다.**
  `snapPoints`(0~1 사이 값 배열, 비어 있으면 스냅 비활성)가 설정돼 있으면, 입력이 멈춘
  프레임(`isDriving`이 true→false로 전환되는 순간)에 한해 `progress`를 가장 가까운
  `snapPoints` 값으로 부드럽게(별도 짧은 보간) 당긴다. 이동 도중에는 스냅이 전혀 개입하지
  않는다 — "연속 각도에서 정지"(기본값)와 "특정 각도에만 스냅"(선택)이 서로 다른 순간에만
  작동해 요구사항 두 개가 충돌하지 않는다.

---

## 6. 장애물 감지(끼임) — 무한 진동·Joint 폭주 방지의 핵심

이 패널은 **물리 힘으로 밀어붙이지 않는다**(킨네마틱 위치/회전 직접 대입). 다음 위치로
전진하기 직전, 그 경로를 `Physics.BoxCast`(또는 목표 자세에서의 `Physics.OverlapBox`)로 먼저
검사해 막혀 있으면 **전진하지 않고 그 자리에 멈춘다.**

이 저장소는 이미 "물리 반동이냐 결정론적 계산이냐"를 기믹마다 명시적으로 골라왔다
(`SeesawSystem`은 순수 물리 반동, `CatapultArm`은 결정론적 계산식). 이 패널은 그 축에서
**"힘을 아예 걸지 않는다"**는 세 번째 선택지를 쓴다:

- `HingeJoint`/`ConfigurableJoint`로 목표 각도까지 힘을 걸어 밀어붙이는 방식은 막힌 물체와
  서로 미는 진동을 만들 수 있다 — 요구사항이 명시적으로 경고하는 "Joint 폭주"가 바로 이
  경로에서 난다.
- 킨네마틱 Transform 직접 대입 + **사전(pre-check) 감지**는 애초에 막힌 물체에 힘을 가하는
  경로 자체가 없으므로, 진동이나 폭주가 물리적으로 발생할 수 없다 — "감지해서 멈춘다"만
  구현하면 안전장치는 설계상 자동으로 성립한다.

```csharp
bool WouldBeBlocked(float candidateProgress)
{
    // candidateProgress를 임시로 적용한 목표 Transform을 계산하고,
    // 그 자세에서 패널 콜라이더가 새로 쓸어들이는 부피 안에
    // (자기 자신 / 현재 라이더 / 설치된 포탈 트리거는 제외하고) 솔리드 콜라이더가 있는지
    // BoxCast/OverlapBox로 확인한다.
}
```

- `isJammed`는 이 검사가 막혔다고 판정한 즉시 true — 별도 유예 프레임 없이 바로 멈춘다(멈춤
  자체는 안전하므로 지연시킬 이유가 없다).
- **반대 방향 입력은 항상 허용한다.** 막힌 방향으로의 전진만 보류되고, `ApplyDrive`가 반대
  부호로 들어오면 `WouldBeBlocked` 재검사 없이 즉시 반응해 빠져나올 수 있어야 한다(그래야
  "끼운 물체를 빼면 다시 움직인다"는 직관이 성립한다) — 정확한 처리는 §13 미확정 3번 참고.
- 정사면체·블록이 끼워져 있는 동안 패널이 그 자리에서 완전히 멈춰 있는 것 자체가 "경사로/
  쐐기로 고정"이라는 주요 활용(정사면체 쐐기, 정육면체 무게 고정)을 그대로 지원한다 — 별도
  "고정 모드"를 만들 필요가 없다.

---

## 7. 라이더 운반

이 저장소는 이미 이동 발판 위 플레이어 속도 처리에서 한 번 실패하고 고친 이력이 있다 —
`PlayerMover`가 접지 중 수평 velocity를 월드 고정값으로 대입해 회전 발판 위에서 자기진동을
일으키던 버그를 `GroundVelocity` 도입으로 해결했다(2026-09-08). 이 패널도 같은 창구를 타면
**같은 버그를 새로 만들지 않는다.** 정확한 연동(패널이 매 틱 자신의 위치/회전 델타를 그
창구에 어떻게 전달하는지)은 구현 세션에서 실제 `PlayerMover` API를 확인하고 결정한다 —
이 문서는 "기존 창구를 반드시 재사용한다"는 방향만 확정하고, 구체 코드는 범위 밖에 둔다
(§14).

---

## 8. 이동 중 포탈 통과 방향/위치 정확성

`SpacePortal`의 텔레포트 관련 계산(`TeleportAcross`, `RefreshCloneTransforms`, `IsInFront`)은
전부 매 `FixedUpdate`마다 포탈의 **그 순간 Transform**을 `transform.InverseTransformPoint`/
`TransformPoint`로 다시 읽는다 — 캐시된 값을 쓰지 않는다. 포탈은 설치 시 패널의 자식
Transform으로 붙으므로, 패널이 움직이면 포탈도 같이 움직이고 통과 계산도 자동으로 최신
위치/회전을 반영한다. **이론상 포탈 쪽에 추가 작업이 필요 없다** — 이게 이 설계 전체가
성립하는 핵심 전제다.

다만 아래는 코드만으로 확신할 수 없는, **실측이 필요한 가정**이다(구현 세션에서 우선
검증할 것):

- 패널이 빠르게 움직이는 동안 포탈 트리거에 걸쳐 있는 오브젝트(클론 렌더링 구간, PRD §3.5의
  "통과 3단계")가 클론 위치도 매끄럽게 같이 따라오는지 — `RefreshCloneTransforms`가 매
  `FixedUpdate` 최신 Transform을 읽으므로 이론상 따라오지만, 패널 이동 속도가 아주 빠르면
  한 틱 동안의 이동량이 커서 클론이 순간이동하듯 튀어 보일 수 있다.
- 실행 순서 문제 — 이 패널의 이동 로직과 `SpacePortal.FixedUpdate`(트리거 판정)의 스크립트
  실행 순서가 정해져 있지 않으면, 어느 한쪽이 한 프레임 묵은 Transform을 참조해 아주 미세한
  위치 오차가 누적될 수 있다. 문제가 실측되면 Script Execution Order로 패널의 이동을
  `SpacePortal`보다 먼저 돌도록 고정한다.
- 회전형(Pivot) 패널이 빠르게 돌아가는 도중 포탈에 걸쳐 있으면, "몸 회전은 안 건드린다"는
  플레이어 통과 규칙(PRD §3.5)과 "위치는 포탈 로컬 좌표계로 변환"이 동시에 성립하는지 —
  이론적으로는 문제없어야 하나(포탈 로컬 좌표계 자체가 패널 회전을 이미 반영), 고속 회전
  경계 케이스는 실측 전까지 미확정으로 둔다.

---

## 9. 고착 시 초기 위치 복구

`isJammed`가 `stuckRecoveryDelay`(기본값 미확정, §13)만큼 연속으로 유지되면 `progress`를
0으로 되돌린다. 정확히 "장애물이 치워질 때까지 기다렸다가 되돌리는지" 아니면 "장애물 존재와
무관하게 그 시간이 지나면 무조건 되돌리는지"는 §13 미확정 5번에서 다룬다. 되돌리는 동작
자체는 즉시 `t=0` Transform으로 스냅하는 순간이동이며(느린 복귀 보간은 선택 사항), 그 위에
설치된 포탈은 §8의 전제에 따라 자동으로 따라간다.

---

## 10. 에디터 시각화

`RailTrackVisual`(`RailCartSystem`)이 이미 확립한 패턴을 재사용한다 — 순수 시각 전용(콜라이더
없음), 씬에서 값을 바꾸면 즉시 다시 그려진다.

- Rail 모드: `startPose`~`endPose`를 잇는 선(또는 얇은 트랙 메쉬)을 그린다.
- Pivot 모드: `pivotPoint`를 중심으로 `pivotAxis` 축 방향 화살표와, `minAngle`~`maxAngle`
  범위를 나타내는 부채꼴(Gizmos.DrawWireArc 상당)을 그린다.

`PortalSurface.OnDrawGizmos`가 이미 그리는 "바깥 법선" 청록 화살표와 겹쳐도 색을 다르게 둬
혼동을 피한다.

---

## 11. Inspector 파라미터 정리

| 파라미터 | 타입 | 설명 |
|---|---|---|
| `mode` | `enum` | Rail / Pivot |
| `startPose` / `endPose` | `Transform` 또는 로컬 오프셋 | Rail 전용 |
| `travelSpeed` | `float` | Rail 전용, 초당 진행도 변화량 상한 |
| `pivotPoint` / `pivotAxis` | `Vector3` | Pivot 전용 |
| `minAngle` / `maxAngle` | `float` | Pivot 전용, 도 단위 |
| `rotationSpeed` | `float` | Pivot 전용, 초당 각속도 상한 |
| `snapPoints` | `float[]` | 비어 있으면 스냅 비활성(기본) |
| `stuckRecoveryDelay` | `float` | 고착 판정 후 초기 위치 복구까지 대기 시간(초) |

---

## 12. 완료 조건 대비 검증

| 완료 조건 | 충족 방식 |
|---|---|
| 이동형·회전형 패널이 각각 정상 작동한다 | `mode` 하나로 분기, `progress` 공유 모델(§3) |
| 패널 이동 중에도 설치된 포탈이 유지된다 | 포탈이 패널의 자식 Transform(PRD §4.4 기존 확정) — 이 패널은 자기 Transform만 옮김(§2) |
| 움직이는 포탈을 플레이어와 물체가 정상적으로 통과할 수 있다 | `SpacePortal`이 매 틱 최신 Transform을 다시 읽음(§8) — 단 고속 이동/회전 경계 케이스는 실측 필요 |
| 패널을 여러 각도에서 멈춰 실제 경사로나 발사대로 사용할 수 있다 | 입력이 끊기면 `progress`가 그 즉시 멈춤, 자체 방전 없음(§4·§5) |
| 물체가 패널에 끼어도 물리 시스템이 폭주하지 않는다 | 킨네마틱 이동 + 전진 전 사전 감지(§6) — 애초에 힘을 걸지 않아 진동 경로 자체가 없음 |

---

## 13. 확정 사항 (Resolved Decisions)

아래 8개 항목은 2026-09-11 사용자와 논의해 모두 확정했다.

1. **Rail 모드 경로의 형태**: **직선 2점만 지원** (`startPose`/`endPose`). 곡선 제어점은
   도입하지 않는다 — 필요해지면 그때 확장(YAGNI).
2. **장애물 판정 대상의 범위**: **전부 솔리드 콜라이더**를 장애물로 취급한다. 별도 마커
   컴포넌트는 두지 않는다.
3. **반대 방향 입력의 즉시 반응 여부**: **즉시 허용**. 막힌 방향으로만 전진이 보류되고,
   반대 부호 입력은 `WouldBeBlocked` 재검사 없이 바로 반응한다(§6 가정 그대로 확정).
4. **스냅 동작 방식**: **멈춘 순간에만** 스냅한다. 이동 도중에는 스냅이 개입하지 않는다(§5
   가정 그대로 확정).
5. **고착 복구 조건**: **장애물이 사라질 때까지 대기**했다가 원위치로 복구한다.
   `stuckRecoveryDelay` 경과와 무관하게, 장애물이 남아있는 동안은 복구하지 않는다.
6. **라이더 운반 방식**: **`PlayerMover`의 `GroundVelocity` 경로를 재사용**한다(부모화 방식은
   채택하지 않음). Pivot 모드에서는 매 틱 `각속도 × (플레이어 위치 − pivotPoint)`로 접선
   속도를 계산해 `GroundVelocity`에 실어준다. 부모화를 배제한 이유: 이 패널은 포탈 자체가
   이미 자신의 자식 Transform인 구조(§8)라, 플레이어까지 부모화하면 회전 중 포탈 통과
   순간(핵심 활용 "포탈 발사 방향 조절") 부모-자식 관계를 해제하는 타이밍 문제가 새로
   생긴다 — 기존에 검증된 `GroundVelocity` 창구를 타는 쪽이 이 조합에서 더 안전하다.
7. **컴포넌트 분리 여부**: **별도 컴포넌트로 분리**한다. `MovablePortalPanel`
   (`RequireComponent(typeof(PortalSurface))`)로 신설하고, `PortalSurface` 자체는 손대지
   않는다.
8. **레버/전력 공급/플레이어 물리력 각각의 구체 입력 구현**: 계속 범위 밖으로 둔다.
   `ApplyDrive(float signedDelta)` 계약만 지키면 방식은 무엇이든 무방하다는 원칙만 확정하고,
   구체 드라이버 구현은 후속 작업(§14)에서 다룬다.

---

## 14. 이 문서의 범위 밖 (후속 작업 대상)

- 레버 / 전력 공급 / 플레이어 물리력 각 드라이버의 구체 구현
- 라이더 운반의 정확한 코드 경로(`PlayerMover` 통합 방식 확정)
- `Assets/SpacePortalSystem/` 내 실제 파일 배치와 클래스명 확정
- `SpacePortal`과의 실행 순서 이슈가 실제로 존재하는지 실측 검증(§8)
- 스냅·고착 복구의 정확한 튜닝값(기본값은 전부 씬 튜닝 대상)

이 항목들은 패널 자체의 설계가 아니므로 별도 문서/후속 논의에서 다룬다.
