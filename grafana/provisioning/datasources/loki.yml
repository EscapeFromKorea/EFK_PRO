apiVersion: 1

datasources:
  - name: Loki (EFK-PRO, local)
    uid: loki_efk_pro
    type: loki
    access: proxy
    # 같은 docker-compose 네트워크 안의 loki 서비스를 서비스명으로 바로 가리킨다.
    # 로컬 자체 완결형이라 외부 URL/계정/토큰이 전혀 필요 없다.
    url: http://loki:3100
    isDefault: true
    editable: false
