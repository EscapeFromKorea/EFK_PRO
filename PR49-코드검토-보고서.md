# PR #49 코드 검토 보고서 — 발견 사항과 해결책

> **대상**: [FEAT] 낙석(FallingRockSystem) 기믹 신설 + 모래시계 도형 3역 분담·감속 램프
> **브랜치**: `test/sksmsaidkeu-v-1.0.1` → `develop` · 커밋 `41395dd`
> **작성일**: 2026-07-31
> **성격**: 머지를 막을 사유는 아님. 머지 후 후속 과제로 처리 가능한 항목들.

---

## 요약

| # | 항목 | 심각도 | 성격 | 권장 시점 |
|---|---|---|---|---|
| 1 | `SlowZone` 죽은 참조 누적 | **중** | 잠재 버그 (배치 규약에만 의존) | 머지 후 우선 처리 |
| 2 | 파편 런타임 생성 비용 | **중하** | 성능 | 플레이테스트 결과 보고 |
| 3 | 주석 드리프트 (`HashSet` → `Dictionary`) | **하** | 문서 정확도 | 아무 때나 |

---

## 1. `SlowZone` 죽은 참조 누적 — 코드가 아닌 배치 규약으로만 막혀 있음

### 문제

`SlowZone`은 감속 중인 대상을 딕셔너리에 들고 있습니다.

```csharp
// Assets/HourglassSystem/SlowZone.cs
private readonly Dictionary<PlayerGravityOverride, FallRamp> affected =
    new Dictionary<PlayerGravityOverride, FallRamp>();
```

항목이 제거되는 경로는 `OnTriggerExit`과 `Deactivate` 둘뿐입니다. 그런데 `OnTriggerExit`은
**파괴되며 나가는 대상을 정리하지 못합니다** — 작성자 본인이 주석에 명시해 두었습니다.

```csharp
private void OnTriggerExit(Collider other)
{
    // 대상이 파괴되며 나가는 경로에서는 other가 이미 죽은 참조로 들어온다(프로퍼티 접근 시 예외).
    // 이 경우 복원은 PlayerGravityOverride가 자기 OnDestroy에서 알아서 한다.
    if (other == null) return;   // ← 여기서 반환. affected 에서 제거되지 않는다.
    ...
}
```

즉 **낙석이 감속 구역 안에서 소멸하면 항목이 하나씩 영구히 쌓입니다.** 낙석 커튼은 계속
스폰되므로 누적 속도가 낮지 않습니다.

현재 이 문제는 **코드가 아니라 배치 규약으로만** 막혀 있습니다:

- `FallingRock.despawnFallDistance`(기본 8)를 구역 볼륨보다 아래로 잡아 "구역을 완전히 통과한 뒤 소멸"시킨다
- 어겼을 때는 `FallingRockSpawner`의 경고 로그와 `stuckTimeout` 경고로만 알린다

### 왜 문제인가

맵 제작 중 스폰 지점이나 감속 구역을 옮기면 이 전제가 조용히 깨집니다. 증상이 즉시
드러나지 않고(경고 로그는 콘솔에 묻히기 쉽습니다) 장시간 플레이에서 메모리와 순회 비용으로만
나타나므로, 원인을 추적하기 어려운 종류의 버그입니다.

> **맵 제작 시 주의**: 낙석 스폰 지점 아래 낙하 경로를 감속 구역 바닥보다 최소 2칸 더
> 아래까지 비워 두세요. 이걸 지키지 못하는 배치가 필요하면 아래 해결책을 먼저 적용해야 합니다.

### 해결책 A — `SlowZone` 자체 정리 (권장)

구역이 스스로 죽은 키를 걷어내게 합니다. 낙석뿐 아니라 **모든 대상**에 적용되고, 배치 규약에
의존하지 않는다는 게 장점입니다.

```csharp
// Assets/HourglassSystem/SlowZone.cs 에 추가

private int pruneCounter;
private readonly List<PlayerGravityOverride> deadKeys = new List<PlayerGravityOverride>();

private void FixedUpdate()
{
    // OnTriggerExit은 "파괴되며 나가는" 대상을 정리하지 못한다(위 주석 참고). 낙석이 구역 안에서
    // 소멸하는 배치에서는 항목이 스폰마다 쌓이므로, 주기적으로 죽은 키를 걷어낸다.
    // 1초에 한 번이면 충분하다 — 이 정리는 정확성이 아니라 누적 방지가 목적이다.
    if (++pruneCounter < 50) return;
    pruneCounter = 0;
    if (affected.Count == 0) return;

    deadKeys.Clear();
    foreach (var pair in affected)
        if (pair.Key == null) deadKeys.Add(pair.Key);   // Unity의 == 오버로드가 파괴된 객체를 잡는다

    foreach (var dead in deadKeys)
        affected.Remove(dead);   // C# 참조 자체는 살아 있어 Remove 가 정상 동작한다

    deadKeys.Clear();
}
```

**주의**: 파괴된 `UnityEngine.Object`는 `== null`이 `true`지만 C# 참조와 해시는 그대로라
`Remove`가 동작합니다. 다만 이 동작에 의존하는 게 불안하다면, 딕셔너리를 통째로 재구성하는
방식이 더 확실합니다.

이 방식은 `PlayerGravityOverride`가 자기 `OnDestroy`에서 이미 상태를 복원하므로 **복원 로직과
충돌하지 않습니다** — 걷어내는 건 순수하게 컬렉션 항목뿐입니다.

### 해결책 B — 낙석이 명시적으로 통보

`SlowZone`에 공개 창구를 열고, 낙석이 소멸 직전에 스스로 알립니다.

```csharp
// SlowZone.cs
/// <summary>대상이 스스로 소멸하기 직전 호출한다. OnTriggerExit이 파괴 경로를 못 잡는
/// 구멍을 대상 쪽에서 메우는 창구다(구역 안에서 소멸하는 낙석 등).</summary>
public void Forget(PlayerGravityOverride body)
{
    if (body != null) affected.Remove(body);
}

// FallingRock.cs
private void OnDestroy()
{
    var body = GetComponent<PlayerGravityOverride>();
    if (body == null) return;
    foreach (var zone in FindObjectsOfType<SlowZone>())
        zone.Forget(body);
}
```

**A를 권장합니다.** B는 `OnDestroy`마다 `FindObjectsOfType`를 도는 비용이 있고, 낙석 외의
대상(플레이어 파편, 향후 추가될 오브젝트)이 같은 실수를 반복할 때 또 손대야 합니다.

---

## 2. 파편 런타임 생성 비용

### 문제

낙석이 부서질 때 파편을 매번 새로 만듭니다.

```csharp
// Assets/FallingRockSystem/FallingRock.cs — Shatter()
for (int i = 0; i < shardCount; i++)
{
    GameObject shard = GameObject.CreatePrimitive(PrimitiveType.Cube);  // ← 매번 생성
    ...
    Rigidbody shardBody = shard.AddComponent<Rigidbody>();
    ...
    shard.AddComponent<FallingRockShard>().lifetime = shardLifetime;
}
```

`GameObject.CreatePrimitive`는 **`MeshFilter` + `MeshRenderer` + `BoxCollider`를 함께 만드는
비교적 무거운 호출**입니다. 기본값 `shardCount = 6`이므로 낙석 하나가 부서질 때마다 게임
오브젝트 6개 + 컴포넌트 24개가 생성되고, `shardLifetime`(1.2초) 뒤 전부 파괴됩니다.

낙석 커튼은 `Lane_1`~`Lane_4` 네 줄로 반복 스폰되는 구조라, 낙석이 동시에 바닥에 닿는
구간에서는 한 프레임에 수십 개가 생길 수 있습니다. **생성·파괴 반복은 GC 압력으로 이어져
프레임 끊김(hitch)으로 나타납니다** — 타이밍 퍼즐 기믹에서 프레임이 튀는 건 체감 난이도에
직접 영향을 줍니다.

### 해결책 — 오브젝트 풀

```csharp
// Assets/FallingRockSystem/FallingRockShardPool.cs (신규)
using System.Collections.Generic;
using UnityEngine;

/// <summary>파편을 미리 만들어 두고 재사용한다. 낙석 커튼은 파편을 초당 수십 개 만들었다
/// 버리므로, 매번 CreatePrimitive로 새로 만들면 GC 압력이 프레임 끊김으로 나타난다 —
/// 타이밍 퍼즐에서 프레임이 튀면 체감 난이도가 직접 바뀐다.</summary>
public class FallingRockShardPool : MonoBehaviour
{
    public static FallingRockShardPool Instance { get; private set; }

    [Tooltip("미리 만들어 둘 파편 수. 동시에 부서지는 낙석 수 x shardCount 보다 넉넉하게.")]
    public int prewarm = 48;

    private readonly Queue<GameObject> idle = new Queue<GameObject>();

    private void Awake()
    {
        Instance = this;
        for (int i = 0; i < prewarm; i++) idle.Enqueue(CreateOne());
    }

    private GameObject CreateOne()
    {
        GameObject shard = GameObject.CreatePrimitive(PrimitiveType.Cube);
        shard.transform.SetParent(transform);
        shard.AddComponent<Rigidbody>();
        shard.AddComponent<FallingRockShard>();
        shard.SetActive(false);
        return shard;
    }

    /// <summary>파편 하나를 꺼내 온다. 풀이 비면 새로 만든다(고갈되어 연출이 사라지는 것보다 낫다).</summary>
    public GameObject Take() => idle.Count > 0 ? idle.Dequeue() : CreateOne();

    /// <summary>수명이 다한 파편을 돌려받는다. FallingRockShard가 Destroy 대신 이걸 호출한다.</summary>
    public void Return(GameObject shard)
    {
        shard.SetActive(false);
        shard.transform.SetParent(transform);
        idle.Enqueue(shard);
    }
}
```

`FallingRock.Shatter()`는 `CreatePrimitive` 대신 `FallingRockShardPool.Instance.Take()`를 쓰고,
`FallingRockShard`는 수명이 끝날 때 `Destroy(gameObject)` 대신 `Return(gameObject)`를 호출하도록
바꿉니다. 풀이 씬에 없으면 기존 경로로 폴백하게 두면 손으로 배치한 낙석도 그대로 동작합니다.

### 더 가벼운 대안

플레이테스트에서 끊김이 실제로 확인되지 않으면 풀링까지 갈 필요가 없습니다. 순서대로 시도해 보세요:

1. **`shardCount`를 6 → 3~4로 낮춘다** — 인스펙터 값만 바꾸면 되고 체감 차이가 작습니다
2. **파편 콜라이더를 제거한다** — 파편은 순수 연출인데 `BoxCollider`가 붙어 있어 물리 연산과
   충돌 판정에 계속 참여합니다. 다만 바닥에서 튀는 그림이 사라지므로, 대신
   `FallingRockShard`가 포물선을 직접 계산하게 해야 합니다
3. **`shardLifetime`을 줄인다** — 동시에 살아 있는 파편 수가 줄어듭니다

---

## 3. 주석 드리프트 — `HashSet` → `Dictionary`

### 문제

`FallingRock.cs`의 클래스 주석이 `SlowZone`의 자료구조를 **`HashSet`이라고 설명**합니다.

```csharp
/// [소멸 지점은 반드시 SlowZone 볼륨보다 아래여야 한다 — 하드 제약]
/// 감속 구역 안에서 Destroy되면 SlowZone.affected(HashSet)에 죽은 참조가 남는다.
///                                              ^^^^^^^^^
```

그런데 **같은 PR에서** `SlowZone.affected`는 `Dictionary<PlayerGravityOverride, FallRamp>`로
바뀌었습니다. 램프 상태(진입 시각·진입 속도)를 바디별로 들고 있어야 해서 생긴 변경입니다.

### 왜 고쳐야 하는가

이 저장소는 주석에 "왜"를 남기는 규약을 강하게 지키고 있고, 실제로 그 주석들이 후속 작업의
근거로 인용됩니다. 자료구조 이름이 틀리면 나중에 이 주석을 읽고 `SlowZone`을 열어 본 사람이
혼란을 겪습니다. 기능에는 영향이 없지만, 이 저장소의 문서 품질 기준에서는 고칠 가치가 있습니다.

### 해결책

```diff
- /// 감속 구역 안에서 Destroy되면 SlowZone.affected(HashSet)에 죽은 참조가 남는다.
+ /// 감속 구역 안에서 Destroy되면 SlowZone.affected(Dictionary)에 죽은 참조가 남는다.
```

**항목 1의 해결책 A를 적용한다면 이 주석 문단 전체를 다시 써야 합니다** — "하드 제약"이
아니라 "권장 배치"로 성격이 바뀌기 때문입니다. 두 작업을 함께 처리하는 걸 권합니다.

---

## 권장 처리 순서

1. **머지를 먼저 한다** — 위 세 항목 중 어느 것도 머지를 막을 사유가 아닙니다.
   (단, 본 검토와 별개로 확인이 필요한 게이트 두 가지가 있습니다: 모래시계 동작 변경에 대한
   원작자 확인, 작성자 체크리스트 9개 중 4개만 완료된 플레이테스트)
2. **항목 1을 해결책 A로 처리한다** — 맵 제작이 본격화되면 배치가 계속 바뀌므로, 규약 의존
   상태를 오래 두지 않는 게 좋습니다. 항목 3의 주석 수정을 여기에 함께 넣습니다.
3. **항목 2는 플레이테스트 결과를 보고 판단한다** — 끊김이 실제로 나타나지 않으면 그대로 두고,
   나타나면 "더 가벼운 대안"부터 순서대로 시도합니다.

---

## 이 보고서의 한계

- **컴파일·실행 검증 없이 정적 코드 읽기만으로 작성했습니다.** 항목 2의 성능 영향은 실제
  프레임 측정이 아니라 코드 구조에서 추론한 것이므로, 플레이테스트 결과가 이 판단보다 우선합니다.
- 항목 1의 누적 속도는 낙석 스폰 간격과 실제 배치에 따라 달라집니다. 현재 씬 배치에서 실제로
  누적이 발생하는지는 확인하지 않았습니다 — 배치상 발생하지 않을 수도 있습니다.
- 제안한 코드는 **적용되지 않은 예시**입니다. `EFK_PRO/`는 수정 금지 대상이라 이 보고서에만
  기록했습니다.
