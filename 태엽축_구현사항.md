# 태엽 축 (Windup Axle) 설계 명세

## 문서 정보

- 대상 기능: 태엽 축 (Windup Axle / Spring Shaft)
- 엔진/버전: Unity 2022.3.62f3 (3D)
- 언어: C#
- 문서 범위: **태엽 축 컴포넌트 자신**의 상태·동작·출력 경계까지만 다룬다.
  레일카, 회전판, 회전 계단 등 태엽 축에 연결될 수 있는 파생 장치의 내부 구현은 **이 문서의 범위 밖**이며, 별도 문서에서 다룬다.
- 목적: 이 문서만으로 Claude Code가 태엽 축 컴포넌트를 구현할 수 있도록, 확정된 사항과 미확정 사항을 명확히 구분해 기술한다.

---

## 1. 원본 기획 요구사항 (참고용 원문)

```
기능 이름: 태엽 축

기능:
토크 상태의 플레이어가 축을 돌리면 회전 에너지를 저장하고
연결된 장난감 장치를 작동시킨다.

요구사항:
- 플레이어의 회전 방향과 회전량을 감지한다.
- 감은 양에 따라 장치의 작동 시간 또는 세기가 달라진다.
- 정방향과 역방향을 구분한다.
  - 정방향: 상승 또는 전진
  - 역방향: 하강 또는 후진
- 저장량 최대치, 충전 속도, 방출 속도, 자연 감쇠를 조절할 수 있어야 한다.
- 플랫폼, 레일카, 회전판, 회전 계단 등 여러 장치에 연결할 수 있어야 한다.
- 현재 저장량과 최대 충전 상태를 발광·회전 속도 등으로 보여준다.

주요 활용:
장난감 레일카, 자유 회전 장난감 판 등 태엽 기믹에 활용하는 기능

완료 조건:
- 조금 감았을 때와 최대로 감았을 때 작동 결과가 다르다.
- 방향을 반대로 감으면 연결 장치도 반대로 작동한다.
- 연결 대상이 바뀌어도 태엽 축을 재사용할 수 있다.
```

---

## 2. 핵심 모델 정의

태엽 축은 **"충전-방전 축적기(Accumulator)"**로 동작한다. 손잡이를 돌리는 즉시 장치가 움직이는 발전기형(패스스루)이 아니라, 에너지를 저장했다가 시간에 걸쳐 스스로 풀어내는 방식이다.

- 플레이어가 돌리는 동안: 회전량이 저장량(`currentCharge`)에 누적된다.
- 플레이어가 손을 뗀 뒤에도: 저장된 `currentCharge`가 방출 속도만큼 계속 소모되며 연결 대상을 구동한다.
- 손을 대고 있지 않아도: 자연 감쇠만큼 저장량이 별도로 새어나간다 (구동 여부와 무관한 손실).

태엽 축이 책임지는 범위는 다음 네 가지로 한정한다.

1. 입력으로 들어온 회전을 방향/양으로 해석한다.
2. 그 결과를 내부 저장량에 반영한다 (충전).
3. 시간에 따라 저장량을 소모한다 (방전 + 자연 감쇠).
4. 현재 상태를 신호로 내보낸다 (연결 대상이 무엇인지는 알지 못한 채).

연결된 장치가 그 신호를 어떻게 해석해 움직이는지는 태엽 축의 관심사가 아니다.

---

## 3. 상태 모델 (State Model)

| 필드 | 타입 | 설명 |
|---|---|---|
| `currentCharge` | `float` | 부호 있는 저장량. 범위: `-maxCharge ~ +maxCharge` |
| `maxCharge` | `float` | 저장량 최대치 (Inspector 조절값) |
| `isWinding` | `bool` | 현재 프레임에 플레이어가 실제로 축을 돌리고 있는 중인지 (토크 상태 여부) |

### `currentCharge`의 부호 규칙

- 양수(+) = 정방향(상승/전진)으로 저장된 양
- 음수(-) = 역방향(하강/후진)으로 저장된 양
- 저장량을 부호 있는 단일 값으로 두는 이유: "반대 방향으로 감으면 기존 충전량을 먼저 상쇄하고, 그다음부터 반대 부호로 쌓인다"는 동작이 별도 분기 처리 없이 자연스럽게 성립하기 때문이다. (실제 스프링을 반대로 감을 때 먼저 풀리는 느낌과 일치)

파생 지표:

- `chargeRatio = |currentCharge| / maxCharge` (범위 0~1) — 시각 피드백 및 출력 세기 계산에 사용

---

## 4. 입력 경계 (Input Boundary)

조작 방식(마우스 드래그 / 키 홀드 / 게임패드 스틱 / XR 그랩 등)은 **이 문서에서 결정하지 않는다.** 어떤 입력 방식을 채택하든 아래 메서드 하나로 값만 넘기면 되도록, 입력 감지와 축의 내부 로직을 분리한다.

```csharp
public void ApplyRotation(float signedDelta)
```

- `signedDelta`: 이번 프레임(또는 이번 호출)에 감지된 부호 있는 회전량. 양수 = 정방향 입력, 음수 = 역방향 입력.
- 입력 방식이 무엇이든 이 메서드를 통해서만 축의 상태에 영향을 준다.
- 이 메서드가 호출되는 프레임 동안 `isWinding = true`로 간주한다.

### 방향 판정 임계값

- `directionDeadzone` (`float`): `|signedDelta|`가 이 값보다 작으면 입력을 무시한다. 미세한 입력 떨림으로 방향이 의도치 않게 바뀌는 것을 방지하기 위함.

---

## 5. 충전 · 방전 · 자연 감쇠

세 가지는 서로 다른 조건에서 발생하는 독립된 속도다.

| 구분 | 파라미터 | 발생 조건 | 의미 |
|---|---|---|---|
| 충전 | `chargeRate` | `isWinding == true`일 때만 | 회전 입력량 → 저장량 증가 |
| 방전 | `dischargeRate` | `currentCharge != 0`이면 항상 (손을 떼도 계속) | 저장량 → 출력 신호로 소모되는, "유효하게 일하는" 소모 |
| 자연 감쇠 | `decayRate` | `currentCharge != 0`이면 항상 | 출력과 무관하게 그냥 새어나가는 손실 |

방전과 자연 감쇠는 동시에 같은 저장량을 깎아먹는 별개의 요인이다. 둘 다 `currentCharge`를 0 쪽으로 수렴시키되, 방전은 출력 신호를 만들어내고 자연 감쇠는 만들어내지 않는다.

의사코드:

```csharp
void Tick(float dt)
{
    if (isWinding)
    {
        // ApplyRotation 호출 시점에 이미 반영됨 (아래 6번 참조)
    }

    if (currentCharge != 0f)
    {
        float sign = Mathf.Sign(currentCharge);
        float totalDrain = (dischargeRate + decayRate) * dt;
        currentCharge = sign * Mathf.Max(0f, Mathf.Abs(currentCharge) - totalDrain);
    }

    isWinding = false; // 다음 프레임에 ApplyRotation이 다시 호출되지 않으면 자동 해제
}
```

---

## 6. 충전 반영 로직

```csharp
public void ApplyRotation(float signedDelta)
{
    if (Mathf.Abs(signedDelta) < directionDeadzone) return;

    currentCharge += signedDelta * chargeRate;
    currentCharge = Mathf.Clamp(currentCharge, -maxCharge, maxCharge);
    isWinding = true;
}
```

- 반대 방향 입력이 들어오면 `currentCharge`가 자연히 0을 향해 줄었다가 부호가 바뀌므로, "먼저 상쇄 후 반대로 쌓임" 동작이 별도 코드 없이 성립한다.

---

## 7. 출력 세기 계산

```csharp
float chargeRatio = Mathf.Abs(currentCharge) / maxCharge;
float outputPower = dischargeCurve.Evaluate(chargeRatio) * Mathf.Sign(currentCharge);
```

- `dischargeCurve` (`AnimationCurve`): `chargeRatio`(0~1) → 출력 배율을 매핑하는 곡선.
  - 선형으로 두면 "소진될 때까지 일정한 힘"
  - 감속형(끝에서 낮아지는) 곡선으로 두면 "풀릴수록 힘이 약해지는" 태엽 특유의 느낌
- 이 곡선 하나로 다음 두 완료 조건을 동시에 만족시킨다.
  - "조금 감았을 때와 최대로 감았을 때 작동 결과가 다르다" → `outputPower`가 `chargeRatio`에 비례
  - "저장량 최대치·충전 속도·방출 속도·자연 감쇠를 조절할 수 있어야 한다" → 전부 Inspector 노출 파라미터

---

## 8. 신호 발신 (Output Contract)

태엽 축은 아래 정보를 매 틱 밖으로 내보내는 것으로 자신의 역할을 마친다. **이 신호를 누가, 어떻게 받아서 쓰는지는 이 문서의 범위 밖이다.**

- 부호 있는 출력 세기 (`outputPower`)
- 현재 충전 비율 (`chargeRatio`)
- 완충 도달 시점 이벤트
- 완전 방전(`currentCharge == 0`) 도달 시점 이벤트

발신 채널의 형태(강타입 인터페이스 / UnityEvent / 둘 다 병행)는 연결 대상 설계와 함께 별도로 다룬다. 이번 문서에서 확정하는 것은 "축이 이 네 가지 정보를 공개한다"는 계약(contract) 자체이며, 수신 측 해석 로직은 포함하지 않는다.

---

## 9. 시각 피드백 (축 자신의 표현)

| 피드백 | 연동 값 |
|---|---|
| 발광(Emission) 세기 | `chargeRatio` |
| 인디케이터 회전 방향 | `Mathf.Sign(currentCharge)` |
| 인디케이터 회전 속도 | `Mathf.Abs(outputPower)` |
| 완충/완전방전 시 별도 연출 | 선택 사항 (미확정, 10번 참조) |

---

## 10. Inspector 파라미터 정리

| 파라미터 | 타입 | 설명 |
|---|---|---|
| `maxCharge` | `float` | 저장량 최대치 |
| `chargeRate` | `float` | 입력 델타 대비 충전 속도 |
| `dischargeRate` | `float` | 초당 유효 방전(출력)량 |
| `decayRate` | `float` | 초당 자연 손실량 |
| `dischargeCurve` | `AnimationCurve` | 충전 비율 → 출력 배율 매핑 |
| `directionDeadzone` | `float` | 방향 판정을 위한 최소 입력 임계값 |

---

## 11. 완료 조건 대비 검증

| 완료 조건 | 충족 방식 |
|---|---|
| 조금 감았을 때와 최대로 감았을 때 작동 결과가 다르다 | `outputPower = dischargeCurve.Evaluate(chargeRatio) * sign`로 세기가 `chargeRatio`에 비례 |
| 방향을 반대로 감으면 연결 장치도 반대로 작동한다 | `currentCharge`의 부호가 곧 `outputPower`의 부호를 결정 |
| 연결 대상이 바뀌어도 태엽 축을 재사용할 수 있다 | 축은 8번의 신호 계약만 발신하고 수신 측 해석에 관여하지 않음 (원칙 수준에서 충족, 실제 발신 채널 구현은 별도 문서에서 확정) |

---

## 12. 미확정 사항 (Open Decisions)

아래 항목은 이번 논의에서 확정되지 않았다. Claude Code에서 구현 전 반드시 확인이 필요하다.

1. **오버차지 처리**: `currentCharge`가 `maxCharge`에 도달한 후에도 같은 방향 입력이 계속 들어오면 단순히 클램프만 할지, 아니면 더 이상 감기지 않는 반발(오버차지 튕김) 피드백을 줄지.
2. **완전 방전 상태에서의 자연 감쇠**: `currentCharge == 0`일 때 자연 감쇠는 적용되지 않는다는 전제(5번 의사코드에서 `if (currentCharge != 0f)`로 이미 반영)가 의도한 것이 맞는지 최종 확인 필요.
3. **영속성(Persistence)**: 씬을 나갔다가 다시 들어와도 `currentCharge`를 유지해야 하는지, 아니면 씬 진입 시 항상 0으로 초기화되는지.
4. **정지 상태에서의 방전 지속 여부**: `isWinding == false`가 되어도(플레이어가 손을 뗀 뒤에도) 방전(`dischargeRate`)은 계속 진행되고 충전(`chargeRate`)만 멈춘다는 규칙(5, 6번 로직에 이미 반영)이 의도한 설계가 맞는지.
5. **입력 감지 방식**: 마우스 드래그 / 키 홀드 / 게임패드 스틱 / XR 그랩 중 무엇으로 `ApplyRotation(float signedDelta)`를 호출할지는 이 문서에서 의도적으로 보류함. 4번 섹션의 계약(`signedDelta` 하나만 받는 메서드)만 지키면 어떤 방식이든 무방.

## 13. 이 문서의 범위 밖 (후속 작업 대상)

- 레일카 / 회전판 / 회전 계단 등 파생 장치 각각의 구현
- 8번 신호 계약을 실제로 어떤 채널(인터페이스 / UnityEvent / 하이브리드)로 발신할지의 구체적 구현
- 다중 연결 대상이 동시에 신호를 구독할 때의 분배 정책
- 실제 입력 감지 스크립트(조작 방식 확정 이후)

이 항목들은 태엽 축 자체의 설계가 아니므로 별도 문서/후속 논의에서 다룬다.
