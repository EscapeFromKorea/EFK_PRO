# 맵 1: DayDream — 낙석(꿈의 파편) 구현 사양서

**테마:** 꿈, 몽환
**키워드:** 꿈, 구름, 하늘, 부드러움
**대상 기믹:** 낙석 = 무너져 내리는 꿈의 파편 (떨어지는 블록)

> **문서 목적:** Claude Code가 이 문서만으로 낙석 기믹을 Unity C#으로 구현할 수 있도록 작성한 사양서.
> **표기 규칙:**
> - `[확정]` 원본 기획 이미지에서 명시된 값.
> - `[TBD]` 원본에 없어 미정인 값. **본 문서는 방침상 제안값을 넣지 않고 비워 둠.** 확정 전 구현 불가.
> - `[ASK]` 구현 착수 전 반드시 사용자(기획자)에게 물어봐야 하는 항목. 임의로 결정 금지.
> - 공통 전제(엔진/단위/도형 규격 등)는 `맵1_협동기믹_구현사양.md`의 섹션 0과 동일하게 적용.

---

## 0. 문서 상태 `[중요]`

- 낙석은 원본에서 **독립 기믹으로 정의된 적이 없음.** 몽환의 모래시계 설명 안에서 "구가 통과해야 하는 장애물"로만 언급됨.
- 따라서 아래 `[확정]` 항목은 **모래시계 항목에 부수적으로 적힌 문장 하나가 전부**이며, 낙석 단독의 수치·규칙은 모두 `[TBD]`.
- 이 기믹은 몽환의 모래시계(`맵1_몽환의모래시계_구현사양.md`)와 **함께 동작하는 종속 기믹**임. 단독으로 완결되지 않음.

---

## 1. 컨셉 `[확정]`
- 무너져 내리는 꿈의 파편. 형태는 떨어지는 블록.

---

## 2. 기믹 (원본 확정 전부) `[확정]`
- 낙석이 떨어지는 구간을 캐릭터가 통과해야 함.
- 세모나 사각형이 몽환의 모래시계를 쳐서 시간을 느리게 만들면, 낙석의 낙하 타이밍이 느려짐.
- 그 느려진 타이밍을 이용해 구 캐릭터가 낙석 사이를 뚫고 굴러서 통과함.

> 즉 낙석의 존재 이유는 **모래시계 슬로우와 구의 이동속도(빠름)를 결합한 타이밍 퍼즐**을 만드는 것. 모래시계 사양서 3장·4.4의 도형 역할 분담과 짝을 이룸.

> **[구현 변경 2026-07-30 — 사용자 확정]** 발동 주체를 **세모 단독**으로 좁혔다. 위 원문(`세모나 사각형이 …쳐서`)과 다르므로 원문은 기록으로 남기고 여기에 부기한다.
> - **세모**: 모래시계를 쳐서 발동. 발동시킨 세모에게 모래시계 본체(`Hourglass_Rock`)가 **밀려나지 않는다.**
> - **네모**: 발동시키지 **못한다.** 대신 모래시계 본체를 **밀어서 위치를 옮긴다**(미는 동안만 움직이고 접촉이 끝나면 그 자리에 고정 — 축 제한 없음).
> - **구**: 발동·밀기 모두 못 한다. 열린 창으로 통과하는 역할만.
>
> **왜**: 원문대로 네모도 발동 가능하게 두면 도형 역할이 둘로 겹쳐(세모=발동, 네모=발동) 협동 분담이 흐려지고, 무엇보다 "모래시계를 원하는 위치로 옮긴다"는 축이 생기지 않는다. 세 도형이 **발동 / 이동 / 통과**를 하나씩 맡는 3역 분담으로 정리했다. 구현은 `HourglassSystem/FallingRockFlip.useShapeGate`가 담당하며, 이 플래그를 끄면 예전처럼 운동량만으로 판정한다(§5 6번 플레이테스트용).

---

## 3. 모래시계 연동 `[확정: 연동 사실 / TBD: 연동 수치]`
- 낙석은 모래시계 슬로우 구역의 영향을 받는 오브젝트임.
- 구현 시 모래시계 사양서 4.1의 "구역 국소 슬로우"(구역 내 Rigidbody의 `drag`↑, `gravity`↓) 대상에 낙석이 포함되어야 함.
- 슬로우가 걸리면 낙석의 낙하 속도가 느려져 통과 가능한 타이밍이 열림.

> **[구현 변경 — `drag`↑ 대신 낙하 속도 상한]** 위 4.1 인용대로 `drag` 배율을 올리는 방식은 **조용히 무효가 된다.** `PlayerGravityOverride`의 drag는 "대상의 기본 drag에 곱하는 배율"인데 씬의 낙석 파편과 플레이어 3종은 전부 기본 drag가 0이라, 배율을 얼마로 줘도 0이다(에러도 안 난다). 그래서 `SlowZone.maxFallSpeed`로 하강 속도를 직접 자른다 — 기본 2 U/s, 구역 밖 자유낙하는 약 10 U/s. 중력 배율(0.5)은 그대로 쓴다.
>
> **[구현 변경 — 감속 중에는 스폰 주기도 늘린다]** 낙하만 늦추면 퍼즐이 **거꾸로 어려워진다.** `SlowZone`은 멀티 물리 동기화를 깨지 않기 위해 `Time.timeScale`을 건드리지 않고 개별 물체의 물리만 늦추므로, 스폰이라는 "출처"는 원래 속도로 계속 돌아간다(유량 보존). 낙석 하나가 몸 높이대를 막는 시간만 `1/속도`로 늘어나 빈 시간이 오히려 줄어든다. 그래서 `FallingRockSpawner.spawnIntervalWhileSlowed`로 감속 중 주기를 따로 둔다 — 주기를 값 하나로 두면 §5 6번("감속 없이는 불가 / 감속 중에는 가능")을 동시에 만족시킬 수 없다.
>
> **[구현 변경 — 진입 램프, 2026-07-30]** 하강 속도 상한을 진입 첫 프레임에 즉시 걸면 10 → 2 U/s로 1프레임에 꺾여 급정지처럼 보인다. `SlowZone.transitionTime`에 걸쳐 진입 속도 → `maxFallSpeed`로 보간해 "느려지는 것이 보이고 나서" 느린 속도로 내려가게 한다.

---

## 4. 구현 사양 `[구현값 확정 2026-07-29 — 원본 미정이라 계산으로 유도]`

> 원본에 값이 없어 매직 넘버를 쓰지 않고, §0 공통 전제(1 Unit = 1m, 구 이속 7 U/s·지름 1 U)와
> `SlowZone` 기본값(중력 0.5배, 하강 상한 2 U/s)에서 **유도**했다. **유도 과정 전체가
> `FallingRockSpawner` 클래스 주석에 계산식 (1)~(6)으로 남아 있다** — 값을 바꾸려면 그 검산을
> 다시 하면 되고, `Tools > FallingRock > Create Rock Curtain (Custom)...` 창이 입력값으로
> 그 계산을 다시 해 §2 성립 여부를 표시한다.

| 파라미터 | 설명 | 값 |
|---|---|---|
| `spawnPositions` | 낙석 스폰 위치 | 4줄, 간격 3 U (= `rockSize` + `gapWidth`), 구역 상단. Tools 메뉴가 생성 |
| `spawnInterval` | 낙석 생성 주기 | **2.0초** (감속 없음) |
| `spawnIntervalWhileSlowed` | 감속 중 생성 주기 | **5.0초** — §3 부기의 유량 보존 문제 때문에 상태별로 나뉘어 있다 |
| `initialFallSpeed` | 스폰 시 하강 속도 | **0** (자유낙하. 구역 밖 도달 속도 약 10.85 U/s) |
| `rockSize` | 낙석 블록 크기 | **1 U** (구 지름과 동일) |
| `rockMass` | 낙석 질량 | **1.5** |
| `rockPattern` | 낙하 패턴 | **고정 주기** (§5 3번 확정) |
| `gapWidth` | 구가 통과할 틈 간격 | **2 U** (구 지름 1 + 좌우 0.5 여유 — §0.4 "방향 제어 어려움" 반영) |
| `onHitBehavior` | 캐릭터 피격 시 처리 | **물리 넉백**(dynamic Rigidbody 충돌) + `extraKnockbackImpulse` **0** + `hitsBeforeRespawn` 초과 시 이벤트만. §5 2번 참고 |
| `despawnRule` | 소멸 규칙 | **소멸**, 낙하 `despawnFallDistance` **8 U** 후 — 구역 높이 6 + 바닥 아래 2로 **구역 밖에서** 소멸(§5 5번) |
| `slowFactorInZone` | 슬로우 구역 내 낙하 감속 | 중력 **×0.5** + 하강 속도 상한 **2 U/s** (`transitionTime` 0.5초에 걸쳐 램프). §3 부기 참고 |

**기본값 4줄에서의 통과 창 검산:** 통로 `L` = (4−1)×3 + 2 = **11.00 U** → 구 통과 시간 11/7 = **1.57초**.

| | 낙하 시간 | 도달 속도 | 낙석이 막는 시간 | 통로가 빈 시간 |
|---|---|---|---|---|
| 감속 없음 | 1.11초 | 10.85 U/s | 0.18초 | **1.82초** |
| 감속 중 | 3.20초 | 2.00 U/s | 1.00초 | **4.00초** |

- 조건 B(감속 중 통과 가능) **성립** — 4.00 ≥ 1.57 (2.5배 여유).
- 조건 A(감속 없이는 불가) **불성립** — 1.82 ≥ 1.57로 감속 없이도 통과된다(여유 0.24초). 즉 현재
  기본값은 "모래시계 필수"가 아니라 "어렵지만 가능"이다. 불가로 만들려면 `spawnInterval`을
  **1.76초 미만**(주석 검산은 1.6)으로 낮추거나 `initialFallSpeed`를 올린다.
  **어느 쪽으로 확정할지가 §5 6번이므로 코드 기본값은 임의로 바꾸지 않았다.**

---

## 5. 확인 필요 (기획 질문) `[ASK]`

낙석은 확정 정보가 거의 없어, 다음을 정해야 구현 가능.
**아래 항목은 Claude Code가 임의로 정하지 말고, 구현 착수 전 반드시 사용자에게 질문할 것.**
(특히 2번·6번은 충돌 처리·레벨 난이도의 뼈대를 좌우하므로 우선 확인.)

1. ~~**낙석 자체가 별도 기믹으로 필요한가**, 아니면 모래시계 퍼즐의 연출 요소로만 쓰는가?~~ **확정: 별도 기믹** — `Assets/FallingRockSystem/` 폴더 신설(2026-07-29, 커밋 `900cb66`).
2. ~~낙석에 맞으면 어떻게 되는가?~~ **확정: 넉백은 물리에 맡기고, 리스폰은 이벤트만 쏜다.** 질량 1.5 dynamic Rigidbody가 부딪히면 물리가 이미 밀어내므로 `extraKnockbackImpulse` 기본 0. 누적 피격이 `hitsBeforeRespawn`을 넘으면 `OnHitThresholdExceeded`(인자 = 맞은 플레이어 Root)만 발화한다 — **리스폰 동작이 없는 것은 미완성이 아니라 확정 사항이다.** 리스폰 시스템이 머지되면 인스펙터에서 배선만 한다.
3. ~~낙하 패턴은 고정 타이밍인가, 랜덤인가, 정해진 웨이브인가?~~ **확정: 고정 타이밍.** 랜덤이면 플레이어가 타이밍을 학습할 수 없어 퍼즐이 아니라 운이 된다.
4. ~~구만 통과 대상인가, 다른 도형도 통과해야 하는 구간이 있는가?~~ **확정: 구만.** §2 부기의 3역 분담(세모=발동 / 네모=이동 / 구=통과) 참고.
5. ~~낙석이 바닥/발판에 쌓이는가(장애물화), 아니면 사라지는가?~~ **확정: 소멸.** 쌓이면 지형이 변해 고정 타이밍 퍼즐이 플레이 도중 깨지고 물리 부하도 계속 늘어난다. 단 낙석은 감속 구역 볼륨 **밖(아래)**에서 소멸해야 한다(구역 안에서 `Destroy`되면 `SlowZone.affected`에 죽은 참조가 스폰마다 누적).
6. **[미해결 — 플레이테스트 필요]** 슬로우가 걸리지 않은 상태에서는 통과가 아예 불가능한 난이도인가(모래시계 필수), 아니면 어려울 뿐 가능한가?
   - 계산상으로는 "감속 없이 불가"가 성립하도록 파라미터를 유도했다(`FallingRockSpawner` 클래스 주석의 검산). **실측은 아직 없다** — `FallingRockFlip.useShapeGate`를 끄고 구 단독 발동으로 비교해야 확인된다. 게이트를 끌 때는 낙석이 협동을 우회해 모래시계를 자기 발동시키므로(낙석 운동량 ≈ 15 ≫ `requiredMomentum` 3.0) 모래시계를 낙석 커튼 아래에서 치울 것.
